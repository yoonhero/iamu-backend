export default {
  Mutation: {
    createAccount: (_, {firstName, lastName, username, email, password}:{firstName: string, lastName: string, username: string, email:string, password:string}) => {
        
    }
  }
}